// src/renderBoard.ts
// No-op render function for sand game board

import type { GameState } from './GameState';

export function renderBoard(target: HTMLElement, gameState: GameState): void {
  // No-op: rendering logic will go here
}
